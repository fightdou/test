RHCSA
安装环境
开机破密码
Linux16最后添加：rd.break console=tty0   按ctrl+x
# mount –o remount,rw /sysroot   设置文件的权限
# chroot /sysroot  
# passwd  设置密码
# touch /.autorelabel
如果不设密码 在进入chroot /sysroot后，进入vim /etc/passwd 把root后面的x删掉保存
在进行touch /.autorelabel
清空防火墙
# iptables -F
# systemctl disable iptables
# systemctl stop iptables
# systemctl status iptables
2.修改主机名
# vim /etc/hostname  
# hostname   查看是否正确
3.配置ip地址
# vim /etc/sysconfig/network-scripts/ifcfg-eth0
DEVICE=eth0
BOOTPROTO=static
ONBOOT=yes
TYPE=Ethernet
NAME=eth0
IPADDR=172.25.X.11    X是0—20里的一个      
NETMASK=255.255.255.0
GATEWAY=172.25.X.254
# systemctl restart network
# ip a 验证
4.配置DNS服务器
# vim /etc/resolv.conf
# nameserver 172.25.0.254  自己的IP
# chattr +i /etc/resolv.conf   加不死权限
# cat /etc/resolv.conf  查看
5.设置selinux
# vim /etc/sysconfig/selinux
# selinux=enforcing
# getenforce  验证状态
6.验证yum仓库
# cd /etc/yum.repos.d/
没有的话 
# vim server.repo
[server]
gpgcheck = 0
enabled = 1
baseurl = http://content.example.com/rhel7.0/x86_64/dvd
name = server
# yum clean all 清空缓存
# yum list 验证yum 仓库
